version: "3.4"
services:
  web:
    build: 
      context: .
      dockerfile: /Python-app/Dockerfile
    image: Martin1196155/pythonapp
    ports:
      - "5000:5000"
  redis:
    image: "redis:alpine"
    
